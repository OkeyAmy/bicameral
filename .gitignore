node_modules/
.env
.env.local

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/lib/

# Next.js
web/.next/
web/out/
web/node_modules/

.DS_Store
*.log

# Cloned reference repos — study material, never shipped
.reference/

# Requests board runtime store — demand data, not source
web/data/

# Internal strategy/build docs — not part of the public submission
idea.md
claude.md
hack.md

# Design-reference screenshots — working material, not a project asset
images/

# TS incremental build cache — a local artifact, never source
*.tsbuildinfo

# Demo video scripts/notes — production planning, not part of the submission
demo/
